不同整数类型之间的转换
d = s;
如果sizeof(d)==sizeof(s)：直接比特位复制
如果sizeof(d)>sizeof(s)：如果s是有符号的，s为正d高位全补0，s为负d高位全补1；如果s是无符号的，d高位全补0
如果sizeof(d)<sizeof(s)：复制s低位内存

所以说下面的转换中z和s的值是一样的：
unsigned int s = 2147483650;
int d;
unsigned int z;
d = s;
z = d;
printf("%u %u", z, s);
